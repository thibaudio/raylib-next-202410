name: Release

on:
  release:
    types: [published]

permissions:
  contents: read

jobs:
  build:
    uses: ./.github/workflows/webassembly.yml
    
  deploy:
    permissions:
      contents: write
    runs-on: ubuntu-latest
    needs: [build]
      
    env:
      PROJECT_NAME: ${{ github.event.repository.name }}
      
    steps:
      - name: Download artifact
        uses: actions/download-artifact@v4
        with:
          name: ${{ needs.build.output.PROJECT_RELEASE_PATH }}
          
      - name: Setup release files
        run: |
          echo "PROJECT_RELEASE_NAME=${{ env.PROJECT_NAME }}_v${{ github.event.release.tag_name }}_wasm" >> $GITHUB_ENV
          mv ${{ needs.build.output.PROJECT_RELEASE_PATH }} ${{ env.PROJECT_RELEASE_NAME }} 
          
      - name: Upload Artifact to Release
        uses: softprops/action-gh-release@v1
        with:
          files: ${{ env.PROJECT_RELEASE_NAME }}
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
